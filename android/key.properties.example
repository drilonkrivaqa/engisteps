# Copy to key.properties (git-ignored). Never commit your real credentials.
# storeFile is resolved relative to android/. Use forward slashes on Windows.
storeFile=
storePassword=
keyAlias=
keyPassword=
